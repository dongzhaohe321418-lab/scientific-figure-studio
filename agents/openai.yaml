interface:
  display_name: "Scientific Figure Studio"
  short_description: "Cross-disciplinary image2 figures with saved PNG and SVG"
  default_prompt: "Use $scientific-figure-studio to research my subject, apply its discipline-specific checks, and save a refined PNG plus a genuinely editable SVG with reopening instructions."
policy:
  allow_implicit_invocation: true
