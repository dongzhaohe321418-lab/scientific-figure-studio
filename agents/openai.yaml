interface:
  display_name: "Scientific Figure Studio"
  short_description: "Evidence-grounded image2 figures with editable sources"
  default_prompt: "Use $scientific-figure-studio to research and create a scientifically accurate, visually refined figure, with faithful editable sources if requested."
policy:
  allow_implicit_invocation: true
